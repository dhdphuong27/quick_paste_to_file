<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Updated Color Resources - Using Second Theme's Color Scheme -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="#1b1c1d"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#282a2c"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#333537"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#404040"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#87CEEB "/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#1177BB"/>
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#E0E0E0"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#888888"/>
    <SolidColorBrush x:Key="ProTipBackgroundBrush" Color="#262627"/>
    <SolidColorBrush x:Key="ProTipBorderBrush" Color="#404040"/>
    <SolidColorBrush x:Key="ExampleBackgroundBrush" Color="#282a2c"/>
    <SolidColorBrush x:Key="ExampleBorderBrush" Color="#404040"/>

    <!-- Additional colors from second theme -->
    <SolidColorBrush x:Key="DarkPrimary" Color="#1b1c1d"/>
    <SolidColorBrush x:Key="DarkSecondary" Color="#282a2c"/>
    <SolidColorBrush x:Key="DarkTertiary" Color="#333537"/>
    <SolidColorBrush x:Key="DarkSurface" Color="#262627"/>
    <SolidColorBrush x:Key="DarkBorder" Color="#404040"/>
    <SolidColorBrush x:Key="DarkText" Color="#E0E0E0"/>
    <SolidColorBrush x:Key="DarkTextMuted" Color="#888888"/>
    <SolidColorBrush x:Key="AccentBlue" Color="#007ACC"/>
    <SolidColorBrush x:Key="AccentPurple" Color="#7E57C2"/>
    <SolidColorBrush x:Key="ErrorRed" Color="#FF5252"/>
    <SolidColorBrush x:Key="LightGray" Color="#E0E0E0"/>

    <!-- Main Container Style -->
    <Style x:Key="MainContainerStyle" TargetType="Border">
        <!--<Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>-->
        <!--<Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>-->
        <Setter Property="BorderThickness" Value="2"/>
        <!--<Setter Property="CornerRadius" Value="12"/>-->
        <Setter Property="Padding" Value="32"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" Opacity="0.3" BlurRadius="12"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style for tip containers -->
    <Style x:Key="TipContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="8"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style for step numbers -->
    <Style x:Key="StepNumberStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Fill" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#007ACC" Direction="0" ShadowDepth="0" Opacity="0.3" BlurRadius="4"/>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- Style for step number text -->
    <Style x:Key="StepNumberTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StepNumberGridStyle" TargetType="Grid">
        <Setter Property="Margin" Value="0,0,12,0"/>
    </Style>

    <!-- Style for main title -->
    <Style x:Key="MainTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Style for introduction text -->
    <Style x:Key="IntroTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="0,0,0,32"/>
    </Style>

    <!-- Style for tip titles -->
    <Style x:Key="TipTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- Style for tip descriptions -->
    <Style x:Key="TipDescriptionStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="LineHeight" Value="20"/>
    </Style>

    <!-- Style for example containers -->
    <Style x:Key="ExampleContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ExampleBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ExampleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,0"/>
    </Style>

    <!-- Style for example text -->
    <Style x:Key="ExampleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        <Setter Property="FontStyle" Value="Italic"/>
    </Style>

    <!-- Style for pro tips container -->
    <Style x:Key="ProTipContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ProTipBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ProTipBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,16,0,0"/>
    </Style>

    <!-- Style for pro tips title -->
    <Style x:Key="ProTipTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
    </Style>

    <!-- Style for pro tips content -->
    <Style x:Key="ProTipContentStyle" TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="LineHeight" Value="18"/>
    </Style>

    <!-- Style for icons -->
    <Style x:Key="HeaderIconStyle" TargetType="FrameworkElement">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Margin" Value="0,0,12,0"/>
    </Style>

    <Style x:Key="ExampleIconStyle" TargetType="FrameworkElement">
        <Setter Property="Width" Value="16"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="ProTipIconStyle" TargetType="FrameworkElement">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ScrollViewer Style for Dark Theme -->
    <Style x:Key="DarkScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>
    <!-- Button Style -->
    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center" 
                                      Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- CheckBox Style -->
    <Style x:Key="ModernCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="0,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" 
                            Width="18" Height="18" 
                            Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="2" 
                            CornerRadius="3">
                            <Path x:Name="CheckMark" 
                              Visibility="Collapsed" 
                              Stroke="{StaticResource AccentBrush}" 
                              StrokeThickness="2" 
                              Data="M 2,6 L 6,10 L 14,2"/>
                        </Border>

                        <ContentPresenter Grid.Column="2" 
                                      VerticalAlignment="Center" 
                                      Content="{TemplateBinding Content}"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>